/* Kaitiaki-Pest trap history compatibility
   Trap popups are handled by index.html. This file intentionally does not
   register a second map click handler, which previously caused a second
   popup to cover the full trap information popup.
*/
(function(){
  // History is rendered by the main popup in index.html.
})();
